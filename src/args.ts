
export default class Args {
    From: string
    To: string
    Output: string

    constructor(from: string, to: string, output: string) {
        this.From = from;
        this.To = to;
        this.Output = output;
    }
}